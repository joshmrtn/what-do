"""The one way out of this process to somebody else's server.

Deliberately empty: a package `__init__` that imports its submodules makes every
importer pay for all of them, and no import statement in the suffering module
shows why.
"""
