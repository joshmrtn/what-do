.env
config/config.yaml
database/
logs/
data/likes.txt
data/dislikes.txt
data/blocklist.json
data/bench-samples.yaml
__pycache__/
*.pyc
.mypy_cache/
.pytest_cache/
*.egg-info/
dist/
.venv/
venv/

# Editor swap and backup files. Not tidiness — `vim .env` writes `.env.swp`
# holding that buffer's contents, so an editor left open during `git add -A`
# is a credential in a commit. `.env` itself being ignored does not cover it.
*.swp
*.swo
*~
